<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Feleves_Feladat_FZW0D1.ViewModels"
             x:Class="Feleves_Feladat_FZW0D1.Views.SpotifyImportPage"
             x:DataType="vm:SpotifyImportViewModel"
             Title="Spotify Import">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource Primary}"/>
                <Setter Property="TextColor" Value="White"/>
                <Setter Property="CornerRadius" Value="25"/>
                <Setter Property="HeightRequest" Value="50"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="Padding" Value="20,0"/>
                <Setter Property="Shadow">
                    <Shadow Brush="Black" Offset="2,2" Opacity="0.3"/>
                </Setter>
            </Style>

            <Style TargetType="Border">
                <Setter Property="StrokeShape" Value="RoundRectangle 25"/>
                <Setter Property="Stroke" Value="{StaticResource Primary}"/>
                <Setter Property="StrokeThickness" Value="2"/>
                <Setter Property="Padding" Value="15,0"/>
                <Setter Property="HeightRequest" Value="50"/>
                <Setter Property="VerticalOptions" Value="Center"/>
                <Setter Property="BackgroundColor" Value="Transparent"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

            <Label Text="Spotify Playlist Import"
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="{StaticResource Primary}"
                   HorizontalOptions="Center" 
                   Margin="0,0,0,10"/>

            <Label Text="{Binding StatusMessage}"
                   HorizontalOptions="Center"
                   TextColor="Gray"
                   HorizontalTextAlignment="Center"/>

            <VerticalStackLayout Spacing="5">
                <Label Text="Playlist URL vagy ID" 
                       TextColor="Gray" 
                       FontSize="12" 
                       Margin="15,0,0,0"/>

                <Border>
                    <Entry Text="{Binding PlaylistUrl}"
                           Placeholder="Pl. https://open.spotify.com/playlist/..."
                           IsEnabled="{Binding IsNotImporting}" 
                           VerticalOptions="Center"
                           TextColor="{AppThemeBinding Light=Black, Dark=White}"/>
                </Border>
            </VerticalStackLayout>

            <ActivityIndicator IsRunning="{Binding IsImporting}"
                               IsVisible="{Binding IsImporting}"
                               Color="{StaticResource Primary}"
                               HorizontalOptions="Center" />

            <Button Text="Importálás indítása"
                    Command="{Binding ImportPlayListCommand}"
                    IsEnabled="{Binding IsNotImporting}"
                    HorizontalOptions="Center"
                    WidthRequest="200"
                    Margin="0,10,0,0"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>